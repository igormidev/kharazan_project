import 'package:flutter/material.dart';

class DesktopBattlefieldView extends StatelessWidget {
  const DesktopBattlefieldView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(color: Colors.deepOrange),
    );
  }
}
