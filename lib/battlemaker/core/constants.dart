class Constants {
  static const Duration changePositionAnimationTime =
      Duration(milliseconds: 300);
  static const Duration changeAttackAnimationTime = Duration(milliseconds: 400);
  static const Duration dasd = Duration(milliseconds: 400);
}
